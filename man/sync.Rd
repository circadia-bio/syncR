% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sync.R
\name{sync}
\alias{sync}
\title{Synchronise data across the Circadia Lab package ecosystem}
\usage{
sync(
  tallie = NULL,
  slumb = NULL,
  zeit = NULL,
  id_col = "participant_id",
  join = c("left", "inner", "full")
)
}
\arguments{
\item{tallie}{A data frame of sociodemographic / questionnaire data, typically
the output of \code{tallieR::export()}. Pass \code{NULL} to omit.}

\item{slumb}{A data frame of sleep diary data, typically the output of
\code{slumbR::export()}. Pass \code{NULL} to omit.}

\item{zeit}{A data frame of actigraphy / circadian metrics, typically the
output of \code{zeitR::export()}. Pass \code{NULL} to omit.}

\item{id_col}{Character. Name of the participant ID column shared across all
input data frames. Defaults to \code{"participant_id"}.}

\item{join}{Character. Type of join to perform when combining sources. One of
\code{"left"} (default), \code{"inner"}, or \code{"full"}.}
}
\value{
A tibble with one row per participant per night (where applicable),
containing columns from all supplied sources, indexed by \code{id_col}.
}
\description{
\code{sync()} is the primary entry point for syncR. It pulls sociodemographic and
questionnaire data from \strong{tallieR}, sleep diary data from \strong{slumbR}, and
actigraphy-derived circadian metrics from \strong{zeitR} into a single unified,
participant-indexed database.

\emph{syncR::sync() — pulling everything into alignment, just like the SCN does.}
}
\examples{
\dontrun{
db <- sync(
  tallie = tallieR::export(),
  slumb  = slumbR::export(),
  zeit   = zeitR::export()
)
}

}
